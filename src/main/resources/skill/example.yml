# skill 文件夹中的 *.yml / *.yaml 按文件名排序加载，每个文件只读取一次。
# 可以将不同装备、不同职业或不同主题技能拆分在多个配置文件中。
signature: 1

items:
  example_necklace:
    name: "&d示例项链"
    skills:
      - skill: ExampleDefenceSkill
        trigger: onDamaged
        target: self

      - skill: ExampleSkillCastSkill
        trigger: onSkillCast
        target: targeted
